import Todo from './todo';

/*export default const Todolist = ({todos,onTodoClick})=>{
	<ul>
		{
			todo.map(todo=>{
			<Todo 
				key={todo.id}
				{...todo}
				onClick={()=>onTodoClick(todo.id)}
			/>
			})
		}
	</ul>
};*/
